# TASK — активное задание архитектор → кодер

> Одно активное задание. После выполнения — заполнить `tasks/REPORT.md`.

## TASK-001: Приёмка выделенного проекта

**Контекст:** проект выделен из `COMFYUI-API-CHAT` (2026-07-09). Часть файлов
перенесена через монтирование из чата, тяжёлые галереи и git — через `MIGRATION.ps1`.

**Цель:** убедиться, что выделенный проект самодостаточен и работоспособен.

**Шаги:**
1. Запустить `MIGRATION.ps1` (если ещё не выполнен): перенос галерей + `git init` + первый коммит.
2. Проверить структуру: `scripts/`, `sessions/` (галереи на месте), канонические файлы.
3. Синтаксис: `C:\Python311\python.exe -m py_compile scripts\batch_scenes.py scripts\lora_workflow.py`.
4. Поднять ComfyUI, прогнать `batch_scenes.py` на 1 сцене (уменьшить `SCENES`/`VARIANTS` для скорости) — проверить, что генерация доходит до файла в `sessions/`.
5. Убедиться, что `.env` на месте и под `.gitignore` (`git check-ignore -v .env`).

**Критерий приёмки:** py_compile без ошибок; хотя бы 1 изображение сгенерировано
в `sessions/`; `git status` чистый (галереи-картинки игнорируются).

**Замечания:**
- Python — системный `C:\Python311`, не venv.
- В `print()` — только ASCII (консоль cp1252).
